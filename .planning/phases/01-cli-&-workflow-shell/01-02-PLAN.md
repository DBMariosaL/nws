---
phase: 01-cli-&-workflow-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-cli-&-workflow-shell/01-DISCOVERY.md
autonomous: true

must_haves:
  truths:
    - "Command pack format and install locations are documented with source links."
    - "Local vs global install behavior is explicitly defined for both OpenCode and Claude Code packs."
  artifacts:
    - path: ".planning/phases/01-cli-&-workflow-shell/01-DISCOVERY.md"
      provides: "Authoritative pack spec summary"
      contains: "OpenCode"
  key_links:
    - from: ".planning/phases/01-cli-&-workflow-shell/01-DISCOVERY.md"
      to: "Official command pack docs"
      via: "source URLs"
      pattern: "https?://"
---

<objective>
Confirm OpenCode and Claude Code command pack formats and install paths.

Purpose: Remove ambiguity about pack structure before implementing pack assets and installer.
Output: A discovery document with verified command pack specifications and source links.
</objective>

<execution_context>
@C:\Users\Referent BIM\.config\opencode/get-shit-done/workflows/execute-plan.md
@C:\Users\Referent BIM\.config\opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-&-workflow-shell/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrieve OpenCode command pack specification</name>
  <files>.planning/phases/01-cli-&-workflow-shell/01-DISCOVERY.md</files>
  <action>
Use webfetch to locate and read the official OpenCode command pack documentation. Capture the required file layout, frontmatter keys, command syntax, and install paths for both local and global installs. Record the exact URLs and any version or format constraints.
  </action>
  <verify>01-DISCOVERY.md contains an "OpenCode" section with at least one official URL and an install path for local and global.</verify>
  <done>OpenCode pack format, required frontmatter keys, and install destinations are documented with sources.</done>
</task>

<task type="auto">
  <name>Task 2: Retrieve Claude Code command pack specification</name>
  <files>.planning/phases/01-cli-&-workflow-shell/01-DISCOVERY.md</files>
  <action>
Use webfetch to locate and read the official Claude Code command pack documentation. Capture required file layout, manifest or frontmatter keys, command syntax, and install paths for local and global installs. Record the exact URLs and any version or format constraints. Add a short comparison section noting any differences vs OpenCode packs.
  </action>
  <verify>01-DISCOVERY.md contains a "Claude Code" section with at least one official URL and an install path for local and global.</verify>
  <done>Claude Code pack format, required metadata, and install destinations are documented with sources.</done>
</task>

<task type="auto">
  <name>Task 3: Compile a local/global install matrix</name>
  <files>.planning/phases/01-cli-&-workflow-shell/01-DISCOVERY.md</files>
  <action>
Add a concise "Install Matrix" section that lists OpenCode vs Claude Code with explicit local and global install destinations and any required parent directories. Include one example copy/install command per tool that matches the documented pack format.
  </action>
  <verify>01-DISCOVERY.md includes an "Install Matrix" section covering local and global installs for both tools.</verify>
  <done>The discovery doc provides a clear local/global install reference usable by the pack installer task.</done>
</task>

</tasks>

<verification>
- Discovery doc lists official sources for both pack formats.
- Local and global install paths are specified for both OpenCode and Claude Code.
</verification>

<success_criteria>
The pack formats and install locations are unambiguous and sourced, enabling implementation without guesswork.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-&-workflow-shell/01-02-SUMMARY.md`
</output>
